introduces the given available resources and calculate the remaining need	


int avail[4]= {1,5,2,0};       // Available Resources 
    int f[n],ans[n],ind=0;
    for(k =0;k<n;k++)
    {
    	f[k]=0;
	}
	int need[n] [m];
	for(i=0;i<n;i++)
	{
		for(j=0;j<m;j++)
		need[i][j]=max[i][j]-alloc[i][j];  //calculating the remaining need 
	}
